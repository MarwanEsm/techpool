export interface IHeadlineProps {
    size: number,
    children: React.ReactNode,
    character: string,
    style?: any
}

export interface IColoredDotProps {
    children: React.ReactNode,
    character?: string,
    size: number
}
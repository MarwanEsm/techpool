export interface IHeadlineProps {
    size: number,
    children: React.ReactNode,
    character?: string,
    className?: string
}

export interface IColoredDotProps {
    children: React.ReactNode,
    character?: string,
    size: number
}